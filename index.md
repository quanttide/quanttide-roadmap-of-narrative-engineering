# 叙事工程建设蓝图

## 当前阶段

叙事工程从案例出发，经过实验验证，正在形成可用的创作工具和标准化的模型文件。

### 已完成

| 层 | 产出 |
|----|------|
| 案例 | 都市言情 style.yaml + motif.yaml + story.yaml，14 章成稿 |
| 实验 | p01-p10，覆盖母题提取/识别/约束/缝隙/风格评审/交叉诊断/组合改法 |
| 工具 | narrative-cli — extract / review / inspire |
| 模型 | Harness 五条评估准则，两步诊断法，pairwise 盲评，L1/L2/L3 分类框架 |

---

## 实验序列

基于 memo 中的知识积累，以下实验按依赖顺序排列。

### E1 — L1 任务人机一致率验证

**目标**：验证 L1 分类的假设——L1 任务的人类-AI 一致率 ≥ 85%。

**背景**：memo 将 7 项任务归入 L1（逻辑断裂检测、事实矛盾、时间线错乱、结构完整性、语法拼写、关键词密度、格式合规），但这个分类是先验的，未经数据验证。需要实证检验。

**方法**：
- 材料：职场言情系列 14 篇（已有评级数据和六维评分）
- AI 评审：用当前 prompt 对 14 篇逐项执行 L1 任务
- 人类评审：3 位评审者对同一批材料执行相同任务
- 分析：逐项计算 Cohen's κ，按一致率重新聚类

**成功标准**：
- L1 任务一致率 ≥ 85% → 分类成立，进入 E2
- L1 中存在一致率 < 70% 的任务 → 将该任务降级为 L2，调整分类

**前置依赖**：无（可直接使用现有数据）

### E2 — L2/L3 边界校准

**目标**：验证 L2 和 L3 的边界划分是否合理。

**背景**：memo 中 L2 包含"表层意象识别"这类可能接近 L1 的任务，L3 包含"整体审美判断"这类已在前序评审中被讨论的任务。需要确认分类是否准确。

**方法**：
- 从 L2 任务中选取"表层意象识别"和"情绪倾向标签"，从 L3 任务中选取"情感冲击力"和"整体审美判断"
- 对同一批材料（E1 的 14 篇），计算 AI-人类一致率
- 如果"表层意象识别"的一致率接近 L1 水平 → 升级到 L1
- 如果"整体审美判断"的 AI 输出与人类判断无相关性 → 确认 L3 分类

**成功标准**：
- 调整后的 L1/L2/L3 分类有实证依据
- 输出正式的分类表（覆盖 memo 中的 19 项任务）

**前置依赖**：E1

### E3 — 密度/舒服维度的操作性定义

**目标**：将"阅读舒适度"从概念转化为可测量的文本特征。

**背景**：前序评审（rating-review2）揭示了原评级方法存在密度偏见，但"阅读舒适度"没有操作性定义。memo 中的认知负荷理论（读者建模方法.md）和期待违背理论提供了可操作的工具：测量阅读流畅性和认知负荷，区分"刻意放松"和"能力不足"。

**方法**：
- 选取 3 组对照篇目：高密度高舒服（4.1 便利店谈心）、高密度冲击型（7.2 公园拥抱）、低密度低冲突（9.1 家里吃火锅、4.3 互相问早）
- 用认知负荷指标（推理需求密度、工作记忆过载概率、回溯重读概率）分析三组差异
- 验证假设：高密度高舒服组的认知负荷指标显著低于高密度冲击型组
- 验证假设：低密度低冲突组的认知负荷指标高于"刻意放松"阈值即可认定为好的松弛

**成功标准**：
- 找到可区分"刻意放松"与"空洞敷衍"的文本特征指标
- 输出可作为 L1/L2 任务的"阅读舒适度检测"方案

**前置依赖**：无（可直接使用评级数据中的篇目）

### E4 — 多学科工具的原型验证（语言学方向）

**目标**：验证修辞结构理论（RST）是否能提高逻辑断裂检测的精度，使其从当前 L1 水平升级为更精细的诊断。

**背景**：memo 中语言学方向提出 RST 分析"比简单的逻辑断裂更精细，能区分因果关系与时序关系"。需要验证这一主张。

**方法**：
- 从 14 篇中标注 RST 关系（前提-主张、背景-叙述、因果、时序）
- 对照组：现有 L1 逻辑断裂检测（模式匹配）
- 实验组：RST 辅助的逻辑断裂检测
- 对比两组在召回率和人类认可率上的差异

**成功标准**：
- RST 辅助组的召回率显著高于对照组（p < 0.05）
- 超出当前 L1 能力的部分可形成 L2 新任务

**前置依赖**：E1（确保现有 L1 基线已测量）

### E5 — 创造性偏离检测原型

**目标**：实现从"异常标注"到"异常类型区分"的升级。

**背景**：memo 读者建模方法.md 提出，AI 检测出的异常应区分为"逻辑异常（无歧义错误）"和"结构异常（可能是有意打破常规）"，后者交人类判断。

**方法**：
- 从 14 篇和未来稿件中收集 AI 检测出的"异常"（逻辑断裂、结构缺失等）
- 用认知负荷指标（阅读流畅性、回读概率）为每个异常标注"偏离程度"
- 在 3 篇有明确刻意偏离（如 7.2 的极端情感密度、4.1 的叙事跳跃）的篇目中验证：这些"异常"的认知负荷指标是否显著低于真正的错误

**成功标准**：
- 能输出带类型的异常报告："逻辑异常"vs"结构异常（置信度 XX%）"
- 人类评审确认率 ≥ 70%

**前置依赖**：E3（认知负荷指标先建立）

### E6 — 评价者偏差的量化

**目标**：为 AI 评审输出附上信度度量。

**背景**：memo 读者建模方法.md 提出应用心理学的信度分析方法——重测信度、评分者间一致性、效度验证。

**方法**：
- 对同一篇文本让 AI 在 3 个不同时间点执行同一组 L2 任务
- 计算重测信度（ICC）：如果 ICC < 0.7，该 L2 任务应标注"当前结果不稳定"
- 对比 AI 评审与人类评审（来自 E1）的相关性作为效度指标

**成功标准**：
- 每个评审输出附信度指标："L2 · 风格一致性 · 置信度 72% · 重测信度 0.81"
- 信度过低的任务自动降级

**前置依赖**：E1、E2

---

## 实验依赖图

```
E1（L1 验证） → E2（L2/L3 校准） → E6（信度量化）
                                       ↑
E3（舒适度指标） → E5（偏离检测） ────┘
                    ↑
E4（RST 验证） ────┘
```

---

## 平台方向

与上述实验平行的工程实现路径：

- n-cli review 命令集成 L1 任务 —— 基于 E1 验证结果
- 评审报告 UI 原型 —— 基于 E2 的分类表，实现 Vision 中定义的 L1 绿/L2 黄/L3 红
- 人类评审信誉系统原型 —— 基于 E6 的信度度量

## 小说方向（延续）

- 跨系列验证：都市言情模型迁移至校园言情
- 契约层正式 schema：style + motif + story 的互锁关系
- 情节推理模块：当前盲区是"发生了什么"的前后逻辑
